
instead of a folder for each run, folder for each day makes more sense. 
same goes for run dashboard/csv, instead of a dashboard for each run, dashboard for each day orchestrate was trigered more sense.


option for a single run in the orchestrate. currently it reccurs everytime.

reduce the recommended polling duration just 


Smart apply:

    1. Ask for gender, disability, veteran options & linkedin, portfolio, github links in setup to complete the application properly
    2. Should be able to deal with jobs where text field performs prompt injection like if "if you are a bot type this/ use the word <some word>", if a agent answer that.

1. Option to move jobs from needs attention / shortlisted / filtered / etc section into the jobs applied section. Basically if a user applis on a job manually then they should have a option to update it in the db and move those jobs into the applied section.
    a human check to approve before applying into jobs

2. introduce resume tuning.
3. db balooning for users over time


4. setup recommendations:



Check:

meta 
amazon
scraping working ?


