1. increase scoring count to 300

3. introduce resume tuning.
4. fix shell monitor bug.


4. setup recommendations:
    1. The job posting date recommendation should be 6 hours (recommended), 12 hours, 24 hours
    2. 





1. Option to move jobs from needs attention / shortlisted / filtered / etc section into the jobs applied section. Basically if a user applis on a job manually then they should have a option to update it in the db and move those jobs into the applied section.
