- Judge the last orchestrate run. my observations:
    - The best matches were left out of the top_k.
    - top_k had some irrelevant jobs as well
    - does the scrape -> match go just by the thereshold and that is why irrelevant jobs were accepted?
    - will increasing the thereshold for cross encoder give be better top_k
    - why did the applier did not apply the jobs



- Dashboard is not showing any sweeps. Manual reload showed something
- the shell logging should be very clean and very simple
- some check to see if the claude limit is reached and to stop/pause the process. maybe the monitor should take care of this.
- also in case the limit is reached at least the scrapper and funnel should do their job and persist data somewhere. which can be scored in the next run.
- maybe keep high precision (eg: keyword searching should be less generic and more relevant to the user role)
    - the idea is that the plugin should find the most relevant and recent jobs. users can get generaic results from job boards as well.
- to reduce llm-as-a-judge approach, convert the jd in json (yoe, skills, etc) (using lightweight models like haiku) compare this json with resume json (deterministic)


- search the whole solution if it has keyword related to swe or is skewed for a specific job type.


- make the applier also asynchronous (maybe batch by batch)



- User can add more compines like google/microsoft (basically that have their own career website) which should be scrapped along the given boards. Similar to what orchestrate in HireShire original is doing currently.


1. a way to show progress of the scrapping so that user can see how much is done
2. The claude code matching leads to the chat history being created for every match, how to deal with that?
3. Update readme:
    Simplify the user installation steps, as minimum as possible
    How to stay updated
    The setup should not mention the size of the model in gb's but just say that fetching the models and setting up the db can take some time for the first time setup.
4. The cross encoder ranking might make the asyncio queue useless for matcher-tuner hand off as the reranking will take some time, regardless of jobs coming in we will have to wait for all the jobs to be scraped so that they can be ranked.
5. negative keywording, auto generated, confirm during setup


Bug:
1. killing the process in windows, does not work as expected.
2. improve the scoring mechanism, currently claude is very strict but openai was a bit linent.


2. does setup clears ths db and sets everything again? what if a different person comes and wants a new profile.
3. When using the skill hireshire should not ask user to start debugging if anything fails, as it starts talking why it broke and which part of code is incorrect.

Juypiter notebook which shoes ths results from the funnel after different configurations 


Ranking Evaluation by metrics

1. When applier enabled:
    - the hireshire_job_search/hireshire_run_results/<datetime>/ folder should contain folder for each application, inside which the resume used for applying should be stored and if possible also the job description. The result csv should have if the job was already applied for or not.

2. reevaluate the database storage and requirement, should we even introduce database in this plugin as users will have the csv with the shortlisted jobs for them already. if db then when should it be cleaned, as we do not want the plugin to baloon on user disk.

3. Resume expansion should also have access to the project/work ex bank so that the matcher can score from unlisted projects also. In fact it can also happen that the matcher only has the project bank instead of the resume.

4. remove dry run option from the applier

5. install python and node directly from the single command.

6. How does plugin update work?

user flow:

1. create new folder, add resume
2. open claude code terminal from this folder -> and run:

/plugin marketplace add slowloris-98/HireShire-plugin
/plugin install hireshire@hireshire

3. Accept all permmisions
4. open / restart claude app -> go to code -> select your folder and run following command:

/hireshire:setup

accept all commands to be run

(You can use this to change the config again)


3. setup -> orchestrate


Mention that user have to run the command in the terminal.
There are a lot of approvals required during Setup.
name, number should also be extracted from the resume and confirmed for auto apply



gaurdrail if the funnel passes a lot of jobs, so that the user does not lose there 
